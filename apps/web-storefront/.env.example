# Real backend API this storefront reads from -- the SAME backend
# already serving the mobile app and every other portal in this
# project, not a separate/duplicated one.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Real, public URL this storefront itself is served from -- used to
# build absolute URLs in the real sitemap.xml and robots.txt.
NEXT_PUBLIC_SITE_URL=http://localhost:3001
