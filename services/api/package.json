{
  "name": "leap-api",
  "version": "0.1.0",
  "private": true,
  "description": "Leap Auto Parts Marketplace \u2014 core backend API (catalog, fitment, cart, order, user, payment, notification).",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "lint": "eslint src --ext .js",
    "test": "node --test test/"
  },
  "dependencies": {
    "@paypal/paypal-server-sdk": "^2.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^22.3.1"
  },
  "devDependencies": {
    "eslint": "^9.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
